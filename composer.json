{
    "require": {
        "samwilson/basildon": "^0.2"
    },
    "scripts": {
        "build": "basildon .",
        "deploy": [
            "@build",
            "netlify deploy --prod -d output"
        ]
    }
}
