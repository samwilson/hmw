{
    "require": {
        "samwilson/basildon": "^1.0"
    },
    "scripts": {
        "build": "basildon .",
        "deploy": [
            "@build",
            "netlify deploy --prod -d output"
        ]
    }
}
