{
    "require": {
        "samwilson/basildon": "^0.3"
    },
    "scripts": {
        "build": "basildon .",
        "deploy": [
            "@build",
            "netlify deploy --prod -d output"
        ]
    }
}
