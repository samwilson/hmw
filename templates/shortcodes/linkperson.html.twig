{% if shortcode.attrs.2 is defined %}
    {% set label = shortcode.attrs.2 %}
{% else %}
    {% set person = database.query('select * from pages where id = "/people/'~shortcode.attrs.1~'"').fetch %}
    {% set label = person.title %}
{% endif %}
<a href="{{ page.link('/people/'~shortcode.attrs.1) }}.html">{{ label }}</a>
